torch>=2.0.0
torchvision>=0.15.0  # Required for pretrained models (EfficientNet, ResNet, etc.)
accelerate>=0.20.0
numpy>=1.24.0
scipy>=1.10.0
scikit-learn>=1.2.0
pandas>=2.0.0
matplotlib>=3.7.0
tqdm>=4.65.0
wandb>=0.15.0
pytest>=7.0.0
pyyaml>=6.0.0
h5py>=3.8.0
safetensors>=0.3.0

# Optional: ONNX export (for model deployment)
# onnx>=1.14.0
# onnxruntime>=1.15.0

# Optional: torch.compile acceleration (--compile flag)
# Note: Triton may have compatibility issues on HPC systems.
# If not installed, training proceeds without compilation.
# triton>=2.0.0

# Development dependencies (linting, formatting)
# ruff>=0.8.0
# pre-commit>=3.5.0
